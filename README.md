# Setup instructions

1. Setup python virtual environment by executing ```virtualenv env```
2. Activate virtual environment by ```source ./env/bin/activate```
3. Install python modules by executing ```pip install -r requirements.txt```


# Run instructions
Run ```python transform-xml.py```